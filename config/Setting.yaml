database:
  mysql:
    host: "localhost"
    port: "3306"
    user: "root"
    password: ""
    database: "mavlinkproject"
    charset: "utf8mb4"

redis:
  host: "localhost"
  port: "6379"
  password: ""
  database: "0"

ai:
  lstm:
    enabled: true              # 启用后端调用 Gas Kick API
    url: ""                    # 填入后即可真实调用
    timeout: 30
    alert_window: 5            # 滑动窗口大小（最近几个时间点）
    alert_threshold: 3         # 报警阈值（窗口内有几个1就触发）
  yolo:
    enabled: true
    url: "http://154265c9.r6.cpolar.cn"
    timeout: 30
    conf: 0.10
    iou: 0.60
    imgsz: 1024
